// Minimal AdminAssignWorkerPage placeholder used by router
import 'package:flutter/material.dart';

class AdminAssignWorkerPage extends StatelessWidget {
  const AdminAssignWorkerPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Assign Worker')),
      body: const Center(child: Text('Admin Assign Worker Page')),
    );
  }
}
